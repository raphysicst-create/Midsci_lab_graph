# 조별 실험 그래프 앱 — 교육학적·프로그램적 제언

작성일: 2026-07-10. 코드베이스 전수 조사(template.html 984줄, build.py, configs 16건, coverage.md 56건 판정) 기반.
위치 참조 갱신: 2026-07-24 — src/ 분리(98d7910)로 template.html 줄 번호가 무효가 되어,
줄 번호 대신 `src/template.js`의 §섹션·함수명으로 재앵커함 (수정 지점은 항상 src/).

현재 앱은 "6개 조가 측정값 입력 → 산점도 → 추세선 발견"이라는 좁고 검증된 모델을 잘 구현했다.
공백은 세 갈래다: (1) 탐구 수업의 앞뒤 단계(예측·해석·기록)가 비어 있음,
(2) 오차·이상치를 다루는 장치가 없음, (3) 산출물 보존·부분 편집·접근성 등 운영 기능 공백.

---

## A. 교육학적 제언

### A1. 예측(Prediction) 단계 추가 — POE 모형 [1순위]

현재 도입 씬은 빈 축 소개뿐(src/template.js §5 renderIntro). 예측-관찰-설명(POE)의 "예측"이
빠져 있어 학생이 데이터를 받아들이기만 하는 구조다.

- 도입 씬에 "결과가 어떤 모양일까?" 선택지 4개(오른쪽 위로 직선 / 위로 휘는 곡선 /
  아래로 내려감 / 평평해짐)를 카드로 제시. 교사가 학급 거수 결과를 클릭해 기록.
- 종합 씬에서 "우리 반 예측 vs 실제" 배지로 재소환.
- config 선택 필드 `prediction: true`(기본 켬). 예측 결과도 localStorage 저장.
- 근거: 예측을 명시적으로 시킨 뒤 실측과 대조할 때 개념 변화가 잘 일어난다는
  POE(White & Gunstone) 전통. 현재 씬 구조(도입→입력→종합)에 정확히 끼워 넣을 자리가 있다.

### A2. 교사 발문(질문 카드) 필드 [1순위]

note는 index 카드에만 보이고 앱 안에는 지도 발문이 없다. 그래프를 띄워 놓고 "무엇을
물을지"가 교사 역량에 전적으로 위임된 상태.

- config에 `questions: ["압력이 2배가 되면 부피는?", …]` 선택 필드.
- 종합 씬 하단 "발문" 토글 → 한 장씩 넘기는 질문 카드.
- 실험별 핵심 관찰 포인트("플래토가 핵심", "기울기 차이", "두 곡선 수렴")가 이미
  note에 산재하므로 이를 발문으로 승격. 발문 문구는 교과서(위키) 내용 범위에서만 작성.

### A3. 오차·이상치를 "가르칠 거리"로 [2순위]

현재 이상치는 감지·표시 없이 추세선 계산에 그대로 들어간다(src/template.js §4
fitTrend). 입력 검증은 음수/무한대 거부뿐(§5 commitInput). 조마다 값이 다른 것이 조별
실험의 고유 강점인데 앱이 그 차이를 활용하지 않는다.

자동 제외가 아니라 **논의 유도** 방향으로:

- 종합 씬에 "추세선에서 먼 점" 하이라이트 토글 — 잔차 상위 점을 점선 원으로 표시하고
  점-선 거리 세그먼트를 그림. R²는 중학 수준 초과이므로 시각적 잔차가 적절.
- 점 클릭 시 "이 점 빼고 다시 그리기"(추세선 재계산, 점은 반투명 유지) — 이상치를
  지우는 게 아니라 그 영향을 눈으로 확인시키는 장치.

### A4. 반복 측정·평균 지원 [2순위]

paper_helicopter는 "3회 평균을 손으로 계산해 입력"(config note). 평균 내기 자체가
데이터 처리 학습 요소인데 앱 밖으로 밀려나 있다.

- config에 `trials: 3` 선택 필드 → 조별 표에 x당 3칸 + 자동 평균 열.
- 그래프에는 평균점(진하게)과 개별 측정(연하게) 표시 — 오차막대의 중학생 버전.

### A5. 변인 어휘 연결 [3순위]

도입 씬 문구를 "조작 변인: {xLabel} / 종속 변인: {yLabel}"로 표기하는 옵션
(`showVariables: true`). 교육과정 탐구 요소 어휘와 그래프 축을 직접 연결.

### A6. 수동 작도 역량과의 병행 — 문서로 [3순위]

앱이 점 찍기·추세선 긋기를 전부 대신하므로 학생의 종이 그래프 작도 역량을 대체하면
안 된다. index 상단 안내(또는 README)에 권장 시나리오를 명시: 예) "학생이 종이에
1회 작도한 뒤, 학급 종합·비교 단계에서 이 도구 사용". 코드 변경 아님.

### A7. 커버리지 확장 — photosynthesis_light 선례 일반화 [장기]

- photosynthesis_light는 "약/중/강 정성 단계 → 전등 거리 정량화"로 부적합군을 편입한
  유일한 방법론 사례. 다른 정성 실험에도 같은 전환이 가능한지 후보 발굴 가치가 있다.
- 부적합 38건 중 "2점 비교"(상태변화 질량, 부력)와 "검출 ○×"는 산점도 모델 밖이지만,
  **막대 비교·범주형 집계라는 2차 앱 모델**이면 커버 가능. 지금 구현하지 않고
  coverage.md에 "2차 모델 후보" 절만 추가해 백로그화.
- 현재 적합 16건은 물리·화학의 정량 관계에 편중 — 생물(정량화 성공 1건)·지구과학
  (모형 1건)은 사실상 미커버라는 점을 인지하고 도구의 적용 범위를 교사에게 투명하게 안내.

---

## B. 프로그램적 제언

> **구현 현황 (2026-07-11)**: B1~B7 전부 구현 완료. 추가로 곡선 실험용
> `trendline: "smooth"`(학급 평균 곡선, 단조 3차 보간)를 신설해 점만 찍던
> 8건(가열·냉각·증발·열평형·비열·로르산·증류·광합성)에 추세선이 생겼다.
> B6의 index 학년 내 정렬은 조사 결과 이미 구현돼 있었음.
> 교육학적 제언(A1~A7)은 미착수 — 백로그.

### B1. 결과 내보내기 [1순위] ✅ 구현됨

현재 CSV/PNG/인쇄 일체 없음 — 수업 결과가 localStorage에 갇힌다. 종합 씬에 버튼 2개:

- **CSV 다운로드**: `state.data` → 조×측정점 표. BOM 붙여 엑셀 한글 호환.
- **PNG 저장**: 인라인 SVG → canvas → `toBlob` 다운로드. 외부 의존성 없이 가능,
  단일 오프라인 HTML 원칙 유지.
- 학생 보고서·학급 게시와 연결되는 최소 기능.

### B2. 조별 지우기 + 실행취소 1단계 [1순위] ✅ 구현됨

작성 당시 "전체 지우기"만 있었음. 한 조 오입력에 전체 삭제는 과하다.
(현 구현: src/template.js §7 doClear)

- 조별 씬 헤더에 "이 조 지우기" 버튼(confirm).
- 전체 지우기 직후 5초간 "되돌리기" 토스트(직전 state.data 스냅숏 1개 보관).

### B3. 접근성 [2순위] ✅ 구현됨

- `@media (prefers-reduced-motion: reduce)`에서 점 비행·축 draw-in 애니메이션 생략
  (값 즉시 반영). 현재는 애니메이션을 끌 방법이 없다.
- SVG에 `role="img"` + `<title>`(축·점 개수 요약), nav 버튼에 `aria-label`.
- 툴팁이 mousemove 전용(작성 당시; 현 구현 src/template.js §8) → 점 focus/tap에도 반응하도록.
- 색약 팔레트·●/○ 마커 구분은 이미 잘 되어 있음(유지).

### B4. localStorage 키를 title → slug 기반으로 [2순위] ✅ 구현됨

작성 당시 키가 `"group_graph::" + title`이라 제목 문구만 다듬어도 저장 데이터가
유실된 것처럼 보였다. (현 구현: src/template.js §1 STORE_KEY — slug 기반 + 구 키 이전)

- 키를 slug 기반으로 전환 + 로드 시 구 title 키가 있으면 1회 마이그레이션.
- CLAUDE.md의 해당 경고 문구 갱신.

### B5. 소스 분리 + 자동 스모크 테스트 [2순위] ✅ 구현됨

template.html 984줄 단일 파일은 **산출물**로는 옳지만 **소스**로는 유지보수 부담.

- `src/template.css` / `src/template.js` / `src/template.skel.html`로 분리하고
  build.py가 인라인 조립(산출물은 지금과 동일한 단일 HTML, 마커 규칙 유지).
- 워크플로 5단계(Playwright 수동 확인)를 `test_smoke.py`로 스크립트화:
  앱 열기 → 입력 → 점 비행 → 종합 → 추세선 → 새로고침 유지.
- diff가 커서 별도 커밋으로 진행할 것.

### B6. build.py 보강 [3순위] ✅ 구현됨

- `sys.exit` 즉사 대신 전체 config 오류를 모아 한 번에 보고.
- `configs/schema.json`(JSON Schema)으로 스키마 문서화 — CLAUDE.md 표와 이중 유지
  대신 표가 스키마를 참조.
- index.html 학년 내 카드 정렬(현재 파일 발견 순 → 주제/제목순).

### B7. 교사 편의 [3순위] ✅ 구현됨 (숫자키 + 🌓 테마 토글 포함)

- 숫자키 1~6 = 해당 조 씬 점프, 0 = 도입, 9 = 종합.
- 프레젠터 리모컨(←/→/Space)은 이미 동작 — 유지.
- 다크모드가 OS 설정 종속뿐이므로 수동 토글 버튼 검토.

---

## 우선순위 요약

| 순위 | 항목 | 성격 |
|---|---|---|
| 1 | A1 예측 단계, A2 발문 카드, B1 내보내기, B2 조별 지우기 | 저비용·고효과, 즉시 |
| 2 | A3 잔차/이상치, A4 반복측정 평균, B3 접근성, B4 저장 키, B5 소스 분리+테스트 | 중기 |
| 3 | A5 변인 표기, A6 병행 지침, B6 빌드 보강, B7 편의 | 여유 시 |
| 장기 | A7 2차 앱 모델(막대 비교·범주형) — 부적합 38건 일부 흡수 | 백로그 |
